name: E2E Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  e2e-test:
    name: Run E2E Tests
    runs-on: ubuntu-24.04
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Install System Dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            cmake \
            ninja-build \
            ccache \
            zsh \
            python3 \
            python3-pip \
            python3-numpy \
            python3-pil \
            ffmpeg
      
      - name: Build Plugin
        run: |
          .github/scripts/build-ubuntu --target ubuntu-x86_64 --config RelWithDebInfo
      
      - name: Build E2E Test Tools
        run: |
          cd build_x86_64
          cmake --build . --target udp_replay
      
      - name: Generate Test Packets
        run: |
          cd tests/e2e
          ./generate_packets.py --frames 250 --format PAL --output test_packets
          ls -lh test_packets/video/PAL/ | head -5
          ls -lh test_packets/audio/PAL/ | head -5
      
      - name: Test UDP Replay Tool
        run: |
          cd tests/e2e
          # Quick test to ensure UDP replay works
          ../../build_x86_64/tests/e2e/udp_replay \
            test_packets/video/PAL 127.0.0.1 11000 780 --verbose
      
      - name: Upload Test Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-artifacts
          path: |
            tests/e2e/test_packets/**/*.bin
            tests/e2e/test_output/**/*
          retention-days: 7
      
      - name: Test Summary
        run: |
          echo "## E2E Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Plugin build successful" >> $GITHUB_STEP_SUMMARY
          echo "✅ UDP replay tool built and tested" >> $GITHUB_STEP_SUMMARY
          echo "✅ Test packets generated successfully" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Test Configuration" >> $GITHUB_STEP_SUMMARY
          echo "- Format: PAL" >> $GITHUB_STEP_SUMMARY
          echo "- Frames: 250 (5 seconds)" >> $GITHUB_STEP_SUMMARY
          echo "- Video Packets: 17000" >> $GITHUB_STEP_SUMMARY
          echo "- Audio Packets: 250" >> $GITHUB_STEP_SUMMARY
