# Test potential infinite loop (should be detectable or cancellable)
# This script deliberately creates an infinite loop

COUNTER = 0

LOOP_START:
COUNTER = COUNTER + 1
EFFECT Sharp Pixels
WAIT 10ms

# Infinite loop - always true condition
IF 1 THEN
    GOTO LOOP_START
ENDIF

# Never reached
STOP
