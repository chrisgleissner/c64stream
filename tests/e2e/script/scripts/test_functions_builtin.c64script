# Test built-in functions
# Validates string, math, and utility functions

# Test 1: String functions - LEFT$
TEXT$ = "HELLO WORLD"
PREFIX$ = LEFT$(TEXT$, 5)
LOG "Left 5: " + PREFIX$
WAIT 100ms

# Test 2: String functions - RIGHT$
SUFFIX$ = RIGHT$(TEXT$, 5)
LOG "Right 5: " + SUFFIX$
WAIT 100ms

# Test 3: String functions - MID$
MIDDLE$ = MID$(TEXT$, 7, 5)
LOG "Middle: " + MIDDLE$
WAIT 100ms

# Test 4: String functions - LEN
LENGTH = LEN(TEXT$)
LOG "Length: " + LENGTH
WAIT 100ms

# Test 5: String functions - CHR$
CHAR_A$ = CHR$(65)
LOG "CHR$(65): " + CHAR_A$
WAIT 100ms

# Test 6: String functions - ASC
CODE = ASC("A")
LOG "ASC(A): " + CODE
WAIT 100ms

# Test 7: Conversion functions - STR$
NUM = 42
NUM_STR$ = STR$(NUM)
LOG "STR$(42): " + NUM_STR$
WAIT 100ms

# Test 8: Conversion functions - VAL
STR_NUM$ = "123.45"
PARSED = VAL(STR_NUM$)
LOG "VAL(123.45): " + PARSED
WAIT 100ms

# Test 9: Math functions - ABS
NEG = -42
POS = ABS(NEG)
LOG "ABS(-42): " + POS
WAIT 100ms

# Test 10: Math functions - INT
FLOAT = 3.7
INTEGER = INT(FLOAT)
LOG "INT(3.7): " + INTEGER
WAIT 100ms

# Test 11: Math functions - RND
RANDOM = RND(100)
LOG "RND(100): " + RANDOM
WAIT 100ms

# Test 12: Math functions - SIN
ANGLE = 1.5708
SINE = SIN(ANGLE)
LOG "SIN(pi/2): " + SINE
WAIT 100ms

# Test 13: Math functions - COS
COSINE = COS(0)
LOG "COS(0): " + COSINE
WAIT 100ms

# Test 14: Math functions - TAN
TANGENT = TAN(0.785398)
LOG "TAN(pi/4): " + TANGENT
WAIT 100ms

# Test 15: Math functions - SQRT
SQUARE = 144
ROOT = SQRT(SQUARE)
LOG "SQRT(144): " + ROOT
WAIT 100ms

# Test 16: Math functions - LOG (natural logarithm)
E_VAL = 2.71828
NAT_LOG = LOG(E_VAL)
LOG "LOG(e): " + NAT_LOG
WAIT 100ms

# Test 17: Math functions - EXP
POWER = EXP(1)
LOG "EXP(1): " + POWER
WAIT 100ms

# Test 18: TIME$ function
CURRENT_TIME$ = TIME$()
LOG "Current time: " + CURRENT_TIME$
WAIT 100ms

# Test 19: Functions in expressions
RESULT = INT(RND(10)) + 1
LOG "Random 1-10: " + RESULT
WAIT 100ms

# Test 20: Nested function calls
NESTED$ = LEFT$(RIGHT$("ABCDEFGH", 6), 3)
LOG "Nested result: " + NESTED$
WAIT 100ms

# Test 21: Functions in conditionals
NAME$ = "TEST"
IF LEN(NAME$) = 4 THEN
    EFFECT Sharp Pixels
ENDIF
WAIT 100ms

# Test 22: String manipulation chain
ORIGINAL$ = "  hello world  "
UPPER$ = ORIGINAL$
LENGTH = LEN(UPPER$)
TRIMMED$ = MID$(UPPER$, 3, 11)
LOG "Manipulated: " + TRIMMED$

STOP
