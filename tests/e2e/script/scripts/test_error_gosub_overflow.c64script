# Test GOSUB without matching RETURN in call stack overflow

GOSUB RECURSIVE

STOP

RECURSIVE:
    EFFECT Sharp Pixels
    WAIT 10ms
    # Call itself recursively - should hit max depth limit
    GOSUB RECURSIVE
    RETURN
