# The plugin's properties.ini takes precedence over scene settings for
# recording keys.  Keep this explicit so a missing current-run WAV is an
# assertion failure, never a stale session fallback.

[recording]
record_video=true
record_audio=true
record_csv=true
