# Enable plugin recording (audio.wav + video.avi) for the click assertions.
# The plugin's properties.ini takes precedence over scene settings for
# recording keys, so the scenario must override it here.

[recording]
record_video=true
record_audio=true
record_csv=true
