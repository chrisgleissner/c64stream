rem Test map default values for missing keys
CONFIG{"port"} = 8080
CONFIG${"host"} = "localhost"
VALUE = CONFIG{"missing"}
TEXT$ = CONFIG${"missing"}
if VALUE = 0 then
    palette "colodore"
endif
if TEXT$ = "" then
    palette "pepto_ntsc"
endif
stop
